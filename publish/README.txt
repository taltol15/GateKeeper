========================================================================
           GateKeeper L (Lite) - Pre-Login USB Enforcement
                  מערכת חסימת USB לתחנות קצה (גרסת Pre-Login)
========================================================================

תיקייה זו מכילה את קבצי ההתקנה של מערכת GateKeeper-L.
גרסה זו מיועדת לארגונים הדורשים חסימה אך ורק במצבים שבהם אין משתמש מחובר
(Boot / Restart / Logoff), תוך התעלמות מנעילת מסך רגילה (Win+L).

------------------------------------------------------------------------
📂 תוכן התיקייה
------------------------------------------------------------------------

1. gatekeeper-l.exe
   קובץ המנוע הראשי (Core Service).
   חובה שקובץ זה יישאר באותה תיקייה עם סקריפטים ההתקנה בעת ההרצה.

2. Setup_GUI.ps1  (מומלץ למשתמשים/טכנאים) 🟢
   ממשק התקנה גרפי נוח.
   כולל כפתור להתקנה מוקשחת (Hardened) וכפתור להסרה מלאה.
   > הוראות שימוש: קליק ימני -> Run with PowerShell.

3. install_prod.ps1 (להפצה ארגונית - SCCM/GPO) 🔒
   סקריפט התקנה "שקט" המיועד לסביבת ייצור (Production).
   * הקשחה (Hardening): סקריפט זה נועל את השירות הרמטית.
   * אזהרה: גם למנהל מקומי (Local Admin) לא תהיה אפשרות לעצור או למחוק את השירות לאחר ההרצה.

4. install_debug.ps1 (לפיתוח ובדיקות בלבד) 🛠️
   סקריפט התקנה המאפשר שליטה מלאה למנהל המערכת.
   > לא מיועד לשימוש אצל לקוחות קצה!

------------------------------------------------------------------------
🚀 מדיניות החסימה (Policy Logic)
------------------------------------------------------------------------
* Boot / Restart: חסום (Blocked) - ברירת מחדל.
* User Login: פתוח (Allowed) - שחרור מלא של התקנים.
* Screen Lock (Win+L): פתוח (Allowed) - התעלמות (המצב נשאר כפי שהיה).
* Logoff / Sign Out: חסום (Blocked) - חסימה מיידית.

------------------------------------------------------------------------
📝 הוראות התקנה
------------------------------------------------------------------------
שיטה א' - ממשק גרפי:
1. וודא ש-gatekeeper-l.exe נמצא ליד הקובץ Setup_GUI.ps1.
2. לחץ קליק ימני על Setup_GUI.ps1 ובחר "Run with PowerShell".
3. בחלון שיפתח, לחץ על "Install & Harden".

שיטה ב' - שורת פקודה (SCCM/Admin):
1. פתח PowerShell כ-Administrator.
2. הרץ את הפקודה: .\install_prod.ps1

------------------------------------------------------------------------
Troubleshooting & Logs
------------------------------------------------------------------------
נתיב הלוגים:
Event Viewer -> Windows Logs -> Application
Source: GateKeeper-L

========================================================================